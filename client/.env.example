# Leave empty in dev — Vite's proxy forwards /api → http://localhost:7100 (see vite.config.js).
# Set to a full URL only for production builds, e.g. https://api.hiwagamakers.com/api
# VITE_API_URL=https://api.hiwagamakers.com/api
